"use client";

import { useEffect, useState } from "react";

type View = "home" | "tasks" | "courses" | "projects" | "people" | "plan" | "path" | "events" | "links";
type Schedule = Record<string, string[]>;
type PlannerCourse = { title: string; credits: number; terms: string[] };
const NAV: Array<[View, string]> = [["home", "This week"], ["tasks", "Tasks"], ["courses", "Courses"], ["projects", "Projects"], ["people", "Faculty"], ["plan", "Program plan"], ["path", "Long-term path"], ["events", "Events"], ["links", "Links"]];
const TERMS = ["Fall 2026", "Spring 2027", "Summer 2027", "Fall 2027", "Spring 2028", "Summer 2028"];
const OPTIONS: PlannerCourse[] = [
  { title: "FSE 560 · AI Engineering Foundations", credits: 3, terms: ["Fall 2026"] }, { title: "FSE 561 · AI Ethics & Social Responsibility", credits: 3, terms: ["Fall 2026"] }, { title: "SER 334 · Operating Systems", credits: 3, terms: ["Fall 2026"] },
  { title: "CSE 571 · Artificial Intelligence", credits: 3, terms: ["Spring 2027", "Fall 2027"] }, { title: "CSE 572 · Data Mining", credits: 3, terms: ["Spring 2027", "Fall 2027"] }, { title: "CSE 574 · Planning & Learning Methods", credits: 3, terms: ["Spring 2027", "Spring 2028"] }, { title: "CSE 575 · Statistical Machine Learning", credits: 3, terms: ["Fall 2027", "Spring 2028"] }, { title: "CSE 578 · Data Visualization", credits: 3, terms: ["Spring 2027", "Fall 2027"] }, { title: "CSE 598 · Perception in Robotics", credits: 3, terms: ["Fall 2027", "Spring 2028"] }, { title: "SER 501 · Advanced Data Structures and Algorithms", credits: 3, terms: ["Spring 2027", "Fall 2027"] }, { title: "SER 515 · Foundations of Software Engineering", credits: 3, terms: ["Spring 2027"] }, { title: "SER 516 · Software Product and Process Management", credits: 3, terms: ["Spring 2027", "Fall 2027"] }, { title: "SER 594 · Software Engineering Topics", credits: 3, terms: ["Summer 2027", "Spring 2028"] }, { title: "CSE 584 / SER 584 · Internship", credits: 3, terms: ["Summer 2027", "Summer 2028"] }, { title: "SER 517 · Engineering Project", credits: 3, terms: ["Summer 2028"] },
];
const INITIAL_PLAN: Schedule = { "Fall 2026": ["FSE 560 · AI Engineering Foundations", "FSE 561 · AI Ethics & Social Responsibility", "SER 334 · Operating Systems"], "Spring 2027": ["CSE 571 · Artificial Intelligence", "CSE 572 · Data Mining"], "Summer 2027": ["SER 594 · Software Engineering Topics"], "Fall 2027": ["CSE 574 · Planning & Learning Methods"], "Spring 2028": ["CSE 598 · Perception in Robotics"], "Summer 2028": ["SER 517 · Engineering Project"] };
const PLAN_META = [["Fall 2026", "CURRENT", "Build the foundation"], ["Spring 2027", "6 CREDITS", "Strengthen the core"], ["Summer 2027", "3 CREDITS", "Apply and document"], ["Fall 2027", "6 CREDITS", "Deepen the concentration"], ["Spring 2028", "6 CREDITS", "Turn evidence into a capstone"], ["Summer 2028", "3 CREDITS", "Finish"]];
const STAGES = ["F-1 · Study", "Post-completion OPT", "STEM OPT", "H-1B / cap-exempt work", "Employer EB-2 / EB-3"];
const PATH = [["2026 → Summer 2028", "F-1 · Study", "Complete the degree and career evidence", "Keep authorized status and turn projects, internships, and course work into verifiable outcomes."], ["Summer 2028 → 2029", "Post-completion OPT", "Get degree-related full-time work", "Use the ISSC timeline and preserve job details, work hours, and official updates."], ["2029 → 2031", "STEM OPT", "Extend the work window and prepare H-1B", "Focus on E-Verify employers able to sign I-983 and discuss annual registration early."], ["2029 → 2034+", "H-1B or cap-exempt work", "Build a renewable work bridge", "A lottery is not controllable; university and research roles are a genuine alternate path."], ["Around 2030 → 2035+", "Employer EB-2 / EB-3", "Discuss sponsorship early", "Ask about PERM policy, tenure thresholds, counsel, and timing—not only a job title."]];
const TASKS = [["SEP 10 · 3 PM", "SER 334 · Module 3 exercise", "Confirm the available window and submission format in Canvas."], ["SEP 11 · 11:59 PM", "FSE 560 · Module 3 quiz", "Classification week. This dashboard tracks timing only—not course answers."], ["SEP 11 · 11:59 PM", "FSE 561 · Lab 1", "Review the assignment requirements and responsible-AI policy before beginning."], ["THIS WEEK", "Career strategy appointment", "Bring a current résumé, a public portfolio, and focused questions."], ["SEPTEMBER", "Turn a project into a case study", "Frame Smart Industrial Control as a measurable systems story."], ["THIS MONTH", "Faculty outreach", "Ask one focused question tied to your prototype and research direction."]];

function Planner({ open, plan, dragged, setDragged, move, remove, close }: { open: boolean; plan: Schedule; dragged: string | null; setDragged: (course: string | null) => void; move: (course: string, term: string) => void; remove: (course: string) => void; close: () => void }) {
  if (!open) return null;
  const assigned = new Set(Object.values(plan).flat());
  const details = (course: string) => OPTIONS.find((item) => item.title === course);
  const creditsFor = (term: string) => (plan[term] ?? []).reduce((total, course) => total + (details(course)?.credits ?? 3), 0);
  return <div className="planner-modal-backdrop" role="presentation" onMouseDown={(event) => { if (event.target === event.currentTarget) close(); }}><section className="course-planner planner-modal" role="dialog" aria-modal="true" aria-labelledby="planner-title"><div className="planner-heading"><div><p className="eyebrow">COURSE CONFIGURATOR</p><h3 id="planner-title">Choose a course, then place it in a term when it is offered.</h3></div><div className="planner-actions"><p>Each course shows its planning availability and credits. Removing it returns it to Available courses; confirm actual offerings with ASU.</p><button className="modal-close" type="button" onClick={close}>Close ×</button></div></div><div className="planner-layout"><aside className="course-pool"><h4>Available courses</h4><p className="planner-help">Select or drag a course, then use only a term that accepts it.</p>{OPTIONS.filter((course) => !assigned.has(course.title)).map((course) => <button key={course.title} type="button" draggable onDragStart={() => setDragged(course.title)} onClick={() => setDragged(course.title)} className={dragged === course.title ? "is-selected" : ""}><span><b>{course.title}</b><small>{course.credits} credits · {course.terms.join(" / ")}</small></span><em>Select</em></button>)}</aside><div className="planner-terms">{TERMS.map((term) => { const canDrop = dragged ? Boolean(details(dragged)?.terms.includes(term)) : false; return <section key={term} className={"planner-term" + (canDrop ? " can-drop" : "")} onDragOver={(event) => { if (canDrop) event.preventDefault(); }} onDrop={() => canDrop && dragged && move(dragged, term)}><header><h4>{term}</h4><strong>{creditsFor(term)} credits</strong></header>{(plan[term] ?? []).map((course) => <article key={course} className="planned-course"><span>{course}<small>{details(course)?.credits ?? 3} credits</small></span><button type="button" onClick={() => remove(course)}>Remove</button></article>)}{dragged && <p>{canDrop ? "Drop here" : "Not listed as offered this term"}</p>}</section>; })}</div></div></section></div>;
}

export default function EnglishDashboard() {
  const [view, setView] = useState<View>("home"); const [done, setDone] = useState<Record<number, boolean>>({}); const [plan, setPlan] = useState<Schedule>(INITIAL_PLAN); const [open, setOpen] = useState(false); const [dragged, setDragged] = useState<string | null>(null); const [stage, setStage] = useState(0); const [scenario, setScenario] = useState("sponsor");
  useEffect(() => { try { const value = localStorage.getItem("asu-dashboard-en-plan"); if (value) setPlan({ ...INITIAL_PLAN, ...JSON.parse(value) }); } catch {} }, []);
  useEffect(() => { try { localStorage.setItem("asu-dashboard-en-plan", JSON.stringify(plan)); } catch {} }, [plan]);
  useEffect(() => { const listener = (event: KeyboardEvent) => { if (event.key === "Escape") setOpen(false); }; window.addEventListener("keydown", listener); return () => window.removeEventListener("keydown", listener); }, []);
  const choose = (next: View) => { setView(next); window.requestAnimationFrame(() => window.scrollTo({ top: 0, behavior: "smooth" })); };
  const move = (course: string, destination: string) => { setPlan((current) => { const next = Object.fromEntries(Object.entries(current).map(([term, list]) => [term, list.filter((item) => item !== course)])) as Schedule; next[destination] = [...(next[destination] ?? []), course]; return next; }); setDragged(null); };
  const remove = (course: string) => { setPlan((current) => Object.fromEntries(Object.entries(current).map(([term, list]) => [term, list.filter((item) => item !== course)])) as Schedule); setDragged(null); };
  return <main><header className="site-header"><a className="brand" href="#top"><span>ASU</span><strong>Ian · Fall 2026</strong></a><nav aria-label="Primary navigation">{NAV.map(([id, label]) => <button key={id} type="button" className={view === id ? "is-active" : ""} onClick={() => choose(id)}>{label}</button>)}<a className="language-toggle" href="/">中文</a></nav></header><div className="page app-shell" id="top">
    {view === "home" && <section className="workspace-panel calendar-panel"><div className="workspace-heading"><div><p className="eyebrow">WEEK AT A GLANCE</p><h2>A calm view of what matters now.</h2></div><p>An operating dashboard for coursework, projects, faculty outreach, and the next career move.</p></div><div className="week-calendar">{[["WED", "10", "SER 334 exercise", "3:00 PM deadline", "deadline"], ["THU", "11", "FSE 560 quiz", "11:59 PM", "deadline"], ["SAT", "13", "FSE 560 modeling", "Assignment checkpoint", "class"], ["SUN", "14", "SER 334 programming", "Module CP3", "deadline"], ["MON", "15", "Portfolio case study", "System narrative draft", "optional"]].map(([day, date, title, detail, tone]) => <article className={"calendar-day" + (day === "SUN" ? " is-today" : "")} key={day}><header><span>{day}</span><strong>{date}</strong>{day === "SUN" && <small>FOCUS</small>}</header><div><div className={"calendar-item " + tone}><strong>{title}</strong><span>{detail}</span></div></div></article>)}</div></section>}
    {view === "tasks" && <section className="workspace-panel"><div className="workspace-heading"><div><p className="eyebrow">OPERATING BOARD</p><h2>Tasks, organized by momentum.</h2></div><p>{Object.values(done).filter(Boolean).length}/{TASKS.length} complete. Changes are stored only in this browser.</p></div><div className="task-group-grid"><article className="task-group"><h3>Priority queue</h3>{TASKS.map(([date, title, detail], index) => <article className={"compact-task " + (done[index] ? "is-done" : "")} key={title}><button type="button" className="task-status" aria-pressed={Boolean(done[index])} onClick={() => setDone((current) => ({ ...current, [index]: !current[index] }))}>{done[index] ? "Done" : "Mark done"}</button><span><small>{date}</small><strong>{title}</strong></span><details><summary>Context</summary><p>{detail}</p></details></article>)}</article><article className="task-group"><h3>How it works</h3><p>This separates deadlines, career work, portfolio evidence, and outreach so the next useful choice is visible.</p><div className="next-step"><small>DESIGN PRINCIPLE</small><strong>Reduce cognitive load without hiding useful context.</strong></div></article></div></section>}
    {view === "courses" && <section className="workspace-panel"><div className="workspace-heading"><div><p className="eyebrow">COURSE CONTROL</p><h2>Academic status without the noise.</h2></div><p>The dashboard tracks logistics. Course work itself remains the student’s own work.</p></div><div className="course-grid">{[["FSE 560", "AI Engineering Foundations", "Classification · Module 3", "Mon · 10:30–11:45 · Tempe"], ["FSE 561", "AI Ethics & Social Responsibility", "Lab 1 preparation", "Mon · 4:30–5:45 · Hybrid"], ["SER 334", "Operating Systems & System Programming", "Module 3 in progress", "Tue / Thu · 1:30–2:45 · Polytechnic"]].map(([code, title, status, schedule], index) => <article className="course-card compact-course" key={code}><div className="course-top"><span>0{index + 1}</span><strong>{status}</strong></div><p className="course-code">{code}</p><h3>{title}</h3><p className="schedule">{schedule}</p><div className="course-week"><small>THIS WEEK</small><strong>Review the individual Canvas item before starting.</strong></div></article>)}</div><div className="faculty-note"><strong>Course reminder:</strong> confirm individual deadlines in Canvas. This is an administrative planning tool, not a source of course answers.</div></section>}
    {view === "projects" && <section className="workspace-panel"><div className="workspace-heading"><div><p className="eyebrow">PARALLEL WORKSTREAMS</p><h2>Build evidence, not just features.</h2></div><p>Each workstream has one visible next move—enough structure for steady progress.</p></div><div className="workstream-grid">{[["PORTFOLIO", "Smart Industrial Control", "A mixed-reality and IoT system connecting Quest 3, Unity, MQTT, and ESP32 sensors.", "Draw the system architecture and define one measurable demo outcome."], ["CAREER", "Role-ready portfolio", "Translate systems integration, automation, and applied AI experience into clear case studies.", "Prepare one public project story for a recruiter or hiring manager."], ["RESEARCH", "Reliable industrial AI", "Use real-time sensor data, digital twins, and human-centered decision support as a research direction.", "Ask a faculty member which small, measurable question is most useful first."]].map(([tag, title, detail, next]) => <article className="workstream-card" key={title}><div className="workstream-top"><span>{tag}</span><strong>In progress</strong></div><h3>{title}</h3><p>{detail}</p><div className="next-step"><small>NEXT</small><strong>{next}</strong></div></article>)}</div></section>}
    {view === "people" && <section className="workspace-panel"><div className="workspace-heading"><div><p className="eyebrow">RESEARCH FIT</p><h2>People to start a conversation with.</h2></div><p>Each card connects a research area to a concrete, respectful outreach question.</p></div><div className="mentor-grid">{[["Rong Pan", "Professor · Industrial Engineering / Data Science", "Reliability, digital twins, trustworthy AI, and advanced manufacturing."], ["Muslum Ozgur Ozmen", "Assistant Professor · SCAI", "IoT security, industrial control, formal methods, and applied cryptography."], ["Yeonjung Lee", "Assistant Teaching Professor", "Applied AI, ML, NLP, and social-network analysis."], ["Vivek Gupta", "Assistant Professor · SCAI", "LLMs, complex-data reasoning, and evaluation."], ["John McIntire", "Faculty · Software Engineering", "Software architecture, engineering process, and systems work."]].map(([name, role, focus]) => <article className="mentor-card" key={name}><p className="mini-label">{role}</p><h3>{name}</h3><div className="focus-list"><span>{focus}</span></div><div className="conversation-starter"><small>CONVERSATION STARTER</small><strong>Which first research question would make this direction small enough to prototype and evaluate well?</strong></div><details className="mentor-outreach"><summary>Why this fit</summary><p>Connect your mixed-reality industrial IoT prototype to a concise technical question and ask for advice on scope and evidence.</p></details></article>)}</div></section>}
    {view === "plan" && <section className="workspace-panel"><div className="workspace-heading"><div><p className="eyebrow">FALL 2026 → SUMMER 2028</p><h2>A two-year program plan.</h2></div><p>A working academic map. Confirm iPOS, availability, prerequisites, and capstone requirements with an advisor.</p></div><div className="program-summary"><div><small>EXPECTED COMPLETION</small><strong>Summer 2028</strong></div><div><small>PROGRAM ROUTE</small><strong>Non-thesis · SER 517</strong></div><div><small>CREDIT TARGET</small><strong>30 credits</strong></div></div><div className="planner-launch-row planner-launch-top"><div><p className="mini-label">PERSONAL TERM CONFIGURATION</p><strong>Set your courses first; the program timeline below updates immediately.</strong></div><button className="planner-launch" type="button" onClick={() => setOpen(true)}>Open course configurator</button></div><div className="requirement-key"><div><strong>12</strong><span>Core requirements</span></div><div><strong>9</strong><span>Concentration requirements</span></div><div><strong>6</strong><span>Approved electives</span></div><div><strong>3</strong><span>Engineering project</span></div></div><div className="program-timeline">{PLAN_META.map(([term, status, focus]) => <article className="program-phase" key={term}><div className="phase-rail"><span/></div><div className="phase-head"><div><p className="mini-label">{status}</p><h3>{term}</h3></div><strong>{focus}</strong></div><div className="planned-courses"><small>PLANNED COURSES</small><div>{(plan[term] ?? []).map((course) => <span key={course}>{course}</span>)}</div></div><div className="phase-grid"><section><small>ACADEMIC FOCUS</small><p>Build a coherent AI engineering and software systems foundation.</p></section><section><small>PROJECT</small><p>Keep one demonstrable output: architecture, demo, evaluation, or case-study evidence.</p></section><section><small>CAREER</small><p>Use the work to strengthen a focused systems and AI story.</p></section></div></article>)}</div><Planner open={open} plan={plan} dragged={dragged} setDragged={setDragged} move={move} remove={remove} close={() => { setOpen(false); setDragged(null); }}/><div className="plan-guardrail"><strong>Before registering:</strong> this draft does not change My ASU or iPOS. Confirm current availability, prerequisites, iPOS eligibility, and SER 517 project approval before enrolling.</div></section>}
    {view === "path" && <section className="workspace-panel"><div className="workspace-heading"><div><p className="eyebrow">2026 → 2035+ · SCENARIO, NOT A PROMISE</p><h2>Long-term work and permanent-residence path.</h2></div><p>A conservative planning map, not legal advice or a guarantee. Verify details with ISSC and qualified counsel.</p></div><div className="immigration-summary"><div><small>WORKING PATH</small><strong>F-1 → OPT / STEM OPT → employer H-1B → employer EB-2 / EB-3</strong></div><div><small>SPONSORSHIP CONVERSATION</small><strong>Within 6–18 months of the first full-time job</strong></div><div><small>KEEP IN MIND</small><strong>Rules, eligibility, and timelines can change.</strong></div></div><section className="stage-selector"><div><p className="mini-label">LONG-TERM PATH</p><h3>Where am I now?</h3><p>Choose your current stage in sequence; arrows show how each stage leads to the next.</p></div><div className="stage-track" role="tablist">{STAGES.map((label, index) => <button type="button" role="tab" aria-selected={stage === index} className={stage === index ? "is-selected" : ""} onClick={() => setStage(index)} key={label}><span>{index + 1}</span><strong>{label}</strong></button>)}</div></section>{stage === 0 && <section className="academic-prep"><div className="academic-prep-heading"><div><p className="mini-label">FROM THIS SEMESTER</p><h3>During the degree: open future options deliberately.</h3></div><p>Graduate with compliant work authorization, clear evidence, and more than one job-search route.</p></div><div className="academic-prep-list">{[["Fall 2026", "Build a measurable résumé and an international-friendly employer list."], ["Spring 2027", "Apply for authorized internships, research, or developer roles."], ["Summer 2027", "Keep approved experience records and a public impact summary."], ["Fall 2027", "Target sponsor candidates and university/research alternatives."], ["Spring → Summer 2028", "Plan Post-OPT with the current ISSC checklist."]].map(([term, action]) => <article className="academic-prep-phase" key={term}><div className="academic-prep-term"><h4>{term}</h4><strong>Build verifiable options</strong></div><div><small>WHAT TO DO</small><p>{action}</p></div><div className="academic-prep-guardrail"><small>COMPLIANCE NOTE</small><p>Check work authorization before any off-campus, paid, unpaid, freelance, or self-employed activity.</p></div></article>)}</div></section>}<section className="scenario-panel"><div className="scenario-heading"><div><p className="mini-label">COMMON DECISION POINTS</p><h3>If this happens, what is the next move?</h3></div><p>Select a scenario to change the planning prompt.</p></div><div className="scenario-choices">{[["sponsor", "An employer will sponsor"], ["lottery", "H-1B was not selected / policy changed"], ["research", "University or research route"], ["perm", "Stable job, no PERM policy"]].map(([id, label]) => <button key={id} type="button" className={scenario === id ? "is-selected" : ""} onClick={() => setScenario(id)}>{label}</button>)}</div><div className="scenario-result"><div><p className="mini-label">{scenario === "sponsor" ? "PRIMARY PATH" : scenario === "lottery" ? "PRESSURE TEST" : scenario === "research" ? "ALTERNATE CAREER DIRECTION" : "MID-TERM CHECK"}</p><h4>{scenario === "sponsor" ? "Confirm the policy in writing" : scenario === "lottery" ? "Keep a legitimate backup active" : scenario === "research" ? "Treat research roles as real jobs" : "Ask about PERM before it is urgent"}</h4><p>{scenario === "sponsor" ? "Verify E-Verify, H-1B registration, and PERM timing with HR—not only a verbal promise." : scenario === "lottery" ? "Seek employers with clear sponsorship practices or suitable cap-exempt roles while preserving authorized time." : scenario === "research" ? "Research staff, university IT, labs, and qualifying nonprofits can be a career direction with different H-1B options." : "A role or H-1B does not automatically mean a green-card process. Ask about thresholds, costs, and counsel."}</p><strong>Next: document the answer, timeline, and person responsible for it.</strong></div></div></section><div className="immigration-timeline">{PATH.slice(stage).map(([period, status, title, action]) => <article className="immigration-step" key={title}><div className="immigration-period"><small>{status}</small><strong>{period}</strong></div><div className="immigration-body"><h3>{title}</h3><p>{action}</p></div><div><small>EVIDENCE TO KEEP</small><p>Official records, job details, status documents, measurable project outcomes, and written employer communication.</p></div><div className="immigration-risk"><small>WATCH FOR</small><p>Changing rules, deadlines, work authorization, employer policy, and travel effects.</p></div></article>)}</div><div className="plan-guardrail"><strong>When to seek advice:</strong> talk to ISSC about student status and OPT; use employer counsel or a qualified immigration attorney for employer immigration strategy.</div></section>}
    {view === "events" && <section className="workspace-panel"><div className="workspace-heading"><div><p className="eyebrow">SHOW UP</p><h2>Useful moments to be present.</h2></div><p>Events are not automatically added to a calendar; decide first whether to RSVP.</p></div><div className="event-list">{[["SEP 16", "Fulton career strategy session", "Bring a résumé, public portfolio, and focused questions."], ["SEP 18", "Venture Devils office hours", "Test one problem statement for Smart Industrial Control."], ["SEP 22", "Faculty / lab research event", "Ask one specific technical question and follow up."], ["OCTOBER", "Career fair preparation", "Use one concise case-study story rather than a generic project list."]].map(([date, title, note]) => <article className="event-row" key={title}><strong>{date}</strong><div><h4>{title}</h4><span>{note}</span></div><span>↗</span></article>)}</div></section>}
    {view === "links" && <section className="workspace-panel"><div className="workspace-heading"><div><p className="eyebrow">OFFICIAL LINKS</p><h2>Useful ASU entry points.</h2></div><p>Course, advising, career, program, and international-student resources in one place.</p></div><div className="quick-grid">{[["My ASU", "https://my.asu.edu/"], ["Canvas", "https://canvas.asu.edu/"], ["Career Services", "https://career.asu.edu/"], ["Fulton career", "https://career.engineering.asu.edu/"], ["International Students", "https://issc.asu.edu/"], ["AI Engineering resources", "https://ai-ms.engineering.asu.edu/resources/"], ["Venture Devils", "https://entrepreneurship.asu.edu/programs/venture-devils/"], ["ASU events", "https://events.asu.edu/"]].map(([label, href]) => <a href={href} target="_blank" rel="noreferrer" key={label}><span>{label}</span><strong>↗</strong></a>)}</div><div className="plan-guardrail"><strong>iPOS:</strong> after signing into My ASU, go to <em>My Programs → your degree program → iPOS</em>. Confirm choices with an advisor before submitting.</div></section>}
    <footer><p>Ian · ASU Fall 2026 Dashboard</p><p>Non-sensitive portfolio demo. Confirm course status in Canvas and immigration details with official sources.</p></footer>
  </div></main>;
}
